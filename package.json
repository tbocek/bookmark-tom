{
  "name": "bookmark-tom",
  "version": "1.0.1",
  "repository": "https://github.com/tbocek/bookmark-tom.git",
  "author": "Thomas Bocek <tom@tomp2p.net>",
  "license": "MIT",
  "scripts": {
    "lint": "npx web-ext lint -s src",
    "build": "npx web-ext build -s src --overwrite-dest"
  },
  "devDependencies": {
    "web-ext": "^8.2.0"
  }
}
